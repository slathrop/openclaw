/**
 * @module
 * @typedef {import('../../agents/skills.js').SkillCommandSpec} SkillCommandSpec
 * @typedef {import('../../channels/plugins/types.js').ChannelId} ChannelId
 * @typedef {import('../../config/config.js').OpenClawConfig} OpenClawConfig
 * @typedef {import('../../config/sessions.js').SessionEntry} SessionEntry
 * @typedef {import('../../config/sessions.js').SessionScope} SessionScope
 * @typedef {import('../templating.js').MsgContext} MsgContext
 * @typedef {import('../thinking.js').ElevatedLevel} ElevatedLevel
 * @typedef {import('../thinking.js').ReasoningLevel} ReasoningLevel
 * @typedef {import('../thinking.js').ThinkLevel} ThinkLevel
 * @typedef {import('../thinking.js').VerboseLevel} VerboseLevel
 * @typedef {import('../types.js').ReplyPayload} ReplyPayload
 * @typedef {import('./directive-handling.js').InlineDirectives} InlineDirectives
 */

/**
 * @typedef {object} CommandContext
 * @property
 */

/**
 * @typedef {object} HandleCommandsParams
 * @property
 */

/**
 * @typedef {object} CommandHandlerResult
 * @property
 */

/**
 * @typedef {object} CommandHandler
 * @property
 */

