/**
 * @module pi-embedded-helpers/types
 * Type definitions for embedded Pi agent helpers.
 */

/**
 * @typedef {object} EmbeddedContextFile
 * @property {string} path
 * @property {string} content
 */

/**
 * @typedef {"auth" | "format" | "rate_limit" | "billing" | "timeout" | "unknown"} FailoverReason
 */
