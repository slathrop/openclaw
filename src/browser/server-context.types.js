/**
 * @module
 * @typedef {import('node:http').Server} Server
 * @typedef {import('./chrome.js').RunningChrome} RunningChrome
 * @typedef {import('./client.js').BrowserTab} BrowserTab
 * @typedef {import('./config.js').ResolvedBrowserConfig} ResolvedBrowserConfig
 * @typedef {import('./config.js').ResolvedBrowserProfile} ResolvedBrowserProfile
 */

/**
 * @typedef {object} ProfileRuntimeState
 * @property
 */

/**
 * @typedef {object} BrowserServerState
 * @property
 */

/**
 * @typedef {object} BrowserRouteContext
 * @property
 */

/**
 * @typedef {object} ProfileContext
 * @property
 */

/**
 * @typedef {object} ProfileStatus
 * @property
 */

/**
 * @typedef {object} ContextOptions
 * @property
 */

