---
phase: 07-security-initial-hardening
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - extensions/*/package.json
  - src/cron/schedule.js
  - apps/macos/**/*.xml
  - CHANGELOG.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Plugin versions are synced to 2026.2.3"
    - "Cron schedule formatters work correctly"
    - "Appcast is updated for 2026.2.3"
    - "Release notes reflect 2026.2.3 changes"
  artifacts:
    - path: "extensions/*/package.json"
      provides: "Synced plugin versions"
      contains: "2026.2.3"
    - path: "src/cron/schedule.js"
      provides: "Fixed schedule formatters"
  key_links:
    - from: "src/cron/schedule.js"
      to: "cron formatting"
      via: "schedule display"
      pattern: "format|display"
---

<objective>
Port chore commits: plugin version sync, cron formatters, appcast update, and release notes

Purpose: Sync plugin versions and fix cron schedule display issues
Output: Four commits matching upstream (SYNC-011, SYNC-012, SYNC-013, SYNC-014)
</objective>

<execution_context>
@/Users/slathrop/.claude/get-shit-done/workflows/execute-plan.md
@/Users/slathrop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-security-initial-hardening/07-RESEARCH.md
</context>

<upstream_workflow>
For each commit in this plan:
1. Fetch and show upstream diff: `git fetch upstream && git show <hash> --stat && git show <hash>`
2. Understand the change (read PR if needed: `gh pr view <number> --repo openclaw/openclaw`)
3. Apply equivalent JavaScript change to local files
4. Run tests: `pnpm test`
5. Run lint: `pnpm check`
6. Commit with matching message: `scripts/committer "<message>" <files...>`
</upstream_workflow>

<tasks>

<task type="auto">
  <name>Task 1: SYNC-011 - Sync plugin versions to 2026.2.3</name>
  <files>extensions/*/package.json</files>
  <action>
Port commit f895c9fba (chore: sync plugin versions to 2026.2.3)

1. Read the upstream diff:
   ```bash
   git fetch upstream
   git show f895c9fba --stat
   git show f895c9fba
   ```

2. Understand the change:
   - Plugin/extension package.json files are updated to version 2026.2.3
   - This is a version sync chore commit

3. Apply the equivalent changes:
   - Find all extension package.json files: `ls extensions/*/package.json`
   - Update version fields to 2026.2.3

4. Run tests and lint:
   ```bash
   pnpm test
   pnpm check
   ```

5. Commit with matching message:
   ```bash
   scripts/committer "chore: sync plugin versions to 2026.2.3" extensions/*/package.json
   ```
  </action>
  <verify>`pnpm test` passes, `pnpm check` passes, `git log -1` shows correct message</verify>
  <done>Commit f895c9fba ported as SYNC-011, plugin versions synced to 2026.2.3</done>
</task>

<task type="auto">
  <name>Task 2: SYNC-012 - Resolve cron schedule formatters</name>
  <files>src/cron/schedule.js</files>
  <action>
Port commit cfdc55134 (fix(mac): resolve cron schedule formatters)

1. Read the upstream diff:
   ```bash
   git show cfdc55134 --stat
   git show cfdc55134
   ```

2. Understand the change:
   - Cron schedule formatters have bugs in how they display schedules
   - This fix resolves issues with schedule formatting/display
   - Despite the (mac) prefix, the core logic is in src/cron/

3. Apply the equivalent JavaScript changes:
   - Update schedule.js with the formatter fixes
   - The fix likely involves date/time formatting or schedule display

4. Run tests and lint:
   ```bash
   pnpm test
   pnpm check
   ```

5. Commit with matching message:
   ```bash
   scripts/committer "fix(mac): resolve cron schedule formatters" src/cron/schedule.js
   ```
  </action>
  <verify>`pnpm test` passes, `pnpm check` passes, `git log -1` shows correct message</verify>
  <done>Commit cfdc55134 ported as SYNC-012, cron schedule formatters work correctly</done>
</task>

<task type="auto">
  <name>Task 3: SYNC-013 - Update appcast for 2026.2.3</name>
  <files>apps/macos/**/*.xml or similar appcast files</files>
  <action>
Port commit 7f95cdac7 (chore(mac): update appcast for 2026.2.3)

1. Read the upstream diff:
   ```bash
   git show 7f95cdac7 --stat
   git show 7f95cdac7
   ```

2. Understand the change:
   - This updates the macOS app appcast (Sparkle update feed) for version 2026.2.3
   - This is a macOS-specific chore commit

3. Check if appcast files exist in this repo:
   - Look for: `ls apps/macos/` or similar
   - The appcast is typically an XML file for Sparkle updates

4. If files exist, apply the changes. If not, skip this commit with documentation.

5. Commit with matching message (if applicable):
   ```bash
   scripts/committer "chore(mac): update appcast for 2026.2.3" <affected files>
   ```

Note: If macOS app files don't exist in this repo, create an empty commit or document
that this commit doesn't apply to the JavaScript-only codebase.
  </action>
  <verify>`pnpm test` passes, `pnpm check` passes, `git log -1` shows correct message or documented skip</verify>
  <done>Commit 7f95cdac7 ported as SYNC-013 or documented as N/A for JS-only codebase</done>
</task>

<task type="auto">
  <name>Task 4: SYNC-014 - Update 2026.2.3 notes</name>
  <files>CHANGELOG.md or docs/changelog.md</files>
  <action>
Port commit 54ddbc466 (chore: update 2026.2.3 notes)

1. Read the upstream diff:
   ```bash
   git show 54ddbc466 --stat
   git show 54ddbc466
   ```

2. Understand the change:
   - This updates release notes/changelog for version 2026.2.3
   - Documentation-only change

3. Apply the equivalent changes:
   - Update CHANGELOG.md or equivalent release notes file
   - Add/update entries for 2026.2.3 release

4. Run tests and lint:
   ```bash
   pnpm test
   pnpm check
   ```

5. Commit with matching message:
   ```bash
   scripts/committer "chore: update 2026.2.3 notes" CHANGELOG.md
   ```
  </action>
  <verify>`pnpm test` passes, `pnpm check` passes, `git log -1` shows correct message</verify>
  <done>Commit 54ddbc466 ported as SYNC-014, release notes updated</done>
</task>

</tasks>

<verification>
After all 4 tasks complete:
1. `git log -4 --oneline` shows four commits in correct order
2. `pnpm test` passes
3. `pnpm check` passes
4. Commits match upstream messages:
   - chore: sync plugin versions to 2026.2.3
   - fix(mac): resolve cron schedule formatters
   - chore(mac): update appcast for 2026.2.3
   - chore: update 2026.2.3 notes
</verification>

<success_criteria>
- 4 commits created matching SYNC-011, SYNC-012, SYNC-013, SYNC-014
- Full test suite passes after each commit
- Plugin versions synced
- Cron formatters fixed
- Release documentation updated
</success_criteria>

<output>
After completion, create `.planning/phases/07-security-initial-hardening/07-05-SUMMARY.md`
</output>
